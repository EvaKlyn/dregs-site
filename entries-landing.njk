---
layout: page.njk
title: entries
---

<h2>Worlds</h2>

<ul>
    {%- for post in collections.world | reverse -%}
    <li>
        <a href="{{ post.url }}">{{ post.data.title }}</a> ({{ post.date.toLocaleDateString('en-us', { weekday:"short", year:"numeric", month:"short", day:"numeric"})}})
    </li>
    {%- endfor -%}
</ul>

<h2>Faces</h2>

<ul>
    {%- for post in collections.character | reverse -%}
    <li>
        <a href="{{ post.url }}">{{ post.data.title }}</a> ({{ post.date.toLocaleDateString('en-us', { weekday:"short", year:"numeric", month:"short", day:"numeric"})}})
    </li>
    {%- endfor -%}
</ul>

<h2>Faces</h2>
